// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

// next.config.js

// next.config.mjs

const nextConfig = {
    images: {
      domains: ['source.unsplash.com'],
    },
  };
  
  export default nextConfig;
  